CREATE TABLE ZAPOSLENIK( 
id LONG NOT NULL GENERATED ALWAYS AS IDENTITY,
ime VARCHAR(250) NOT NULL, 
prezime VARCHAR(250) NOT NULL, 
korisnicko_ime VARCHAR(250) NOT NULL unique, 
slika VARCHAR(250) NOT NULL, 
datum_rodjenja DATE NOT NULL, 
PRIMARY KEY(id) 
);

CREATE TABLE KORISNIK( 
id LONG NOT NULL GENERATED ALWAYS AS IDENTITY,
ime VARCHAR(250) NOT NULL, 
prezime VARCHAR(250) NOT NULL, 
korisnicko_ime VARCHAR(250) NOT NULL unique, 
adresa VARCHAR(250) NOT NULL, 
datum_rodjenja DATE NOT NULL, 
PRIMARY KEY(id) 
);

CREATE TABLE PROIZVOD( 
id LONG NOT NULL GENERATED ALWAYS AS IDENTITY,
naziv VARCHAR(250) NOT NULL, 
opis VARCHAR(250) NOT NULL, 
cijena DECIMAL(20,2) NOT NULL, 
slika VARCHAR(250) NOT NULL, 
kolicina INTEGER NOT NULL, 
PRIMARY KEY(id) 
);

CREATE TABLE ZIVOTINJE( 
id LONG NOT NULL GENERATED ALWAYS AS IDENTITY,
vrsta VARCHAR(250) NOT NULL, 
pasmina VARCHAR(250) NOT NULL, 
spol VARCHAR(250) NOT NULL unique, 
slika VARCHAR(250) NOT NULL, 
kontakt DATE NOT NULL, 
godine INTEGER NOT NULL, 
PRIMARY KEY(id) 
);

insert into zaposlenik(ime,prezime,korisnicko_ime,slika,datum_rodjenja) VALUES 
('Ivo','Ivić','zaposlenik','zaposlenik.jpg','1999-12-01');
